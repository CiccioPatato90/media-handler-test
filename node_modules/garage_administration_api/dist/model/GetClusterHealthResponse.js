"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;
var _ApiClient = _interopRequireDefault(require("../ApiClient"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { "default": e }; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); } /**
 * Garage administration API
 * Administrate your Garage cluster programatically, including status, layout, keys, buckets, and maintainance tasks.  *Disclaimer: This API may change in future Garage versions. Read the changelog and upgrade your scripts before upgrading. Additionnaly, this specification is early stage and can contain bugs, so be careful and please report any issues on our issue tracker.*
 *
 * The version of the OpenAPI document: v2.1.0
 * Contact: garagehq@deuxfleurs.fr
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */
/**
 * The GetClusterHealthResponse model module.
 * @module model/GetClusterHealthResponse
 * @version v2.1.0
 */
var GetClusterHealthResponse = /*#__PURE__*/function () {
  /**
   * Constructs a new <code>GetClusterHealthResponse</code>.
   * @alias module:model/GetClusterHealthResponse
   * @param connectedNodes {Number} the nubmer of nodes this Garage node currently has an open connection to
   * @param knownNodes {Number} the number of nodes this Garage node has had a TCP connection to since the daemon started
   * @param partitions {Number} the total number of partitions of the data (currently always 256)
   * @param partitionsAllOk {Number} the number of partitions for which we are connected to all storage nodes responsible of storing it
   * @param partitionsQuorum {Number} the number of partitions for which a quorum of write nodes is available
   * @param status {String} One of `healthy`, `degraded` or `unavailable`: - `healthy`: Garage node is connected to all storage nodes - `degraded`: Garage node is not connected to all storage nodes, but a quorum of write nodes is available for all partitions - `unavailable`: a quorum of write nodes is not available for some partitions
   * @param storageNodes {Number} the number of storage nodes currently registered in the cluster layout
   * @param storageNodesUp {Number} the number of storage nodes to which a connection is currently open
   */
  function GetClusterHealthResponse(connectedNodes, knownNodes, partitions, partitionsAllOk, partitionsQuorum, status, storageNodes, storageNodesUp) {
    _classCallCheck(this, GetClusterHealthResponse);
    GetClusterHealthResponse.initialize(this, connectedNodes, knownNodes, partitions, partitionsAllOk, partitionsQuorum, status, storageNodes, storageNodesUp);
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  return _createClass(GetClusterHealthResponse, null, [{
    key: "initialize",
    value: function initialize(obj, connectedNodes, knownNodes, partitions, partitionsAllOk, partitionsQuorum, status, storageNodes, storageNodesUp) {
      obj['connectedNodes'] = connectedNodes;
      obj['knownNodes'] = knownNodes;
      obj['partitions'] = partitions;
      obj['partitionsAllOk'] = partitionsAllOk;
      obj['partitionsQuorum'] = partitionsQuorum;
      obj['status'] = status;
      obj['storageNodes'] = storageNodes;
      obj['storageNodesUp'] = storageNodesUp;
    }

    /**
     * Constructs a <code>GetClusterHealthResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetClusterHealthResponse} obj Optional instance to populate.
     * @return {module:model/GetClusterHealthResponse} The populated <code>GetClusterHealthResponse</code> instance.
     */
  }, {
    key: "constructFromObject",
    value: function constructFromObject(data, obj) {
      if (data) {
        obj = obj || new GetClusterHealthResponse();
        if (data.hasOwnProperty('connectedNodes')) {
          obj['connectedNodes'] = _ApiClient["default"].convertToType(data['connectedNodes'], 'Number');
        }
        if (data.hasOwnProperty('knownNodes')) {
          obj['knownNodes'] = _ApiClient["default"].convertToType(data['knownNodes'], 'Number');
        }
        if (data.hasOwnProperty('partitions')) {
          obj['partitions'] = _ApiClient["default"].convertToType(data['partitions'], 'Number');
        }
        if (data.hasOwnProperty('partitionsAllOk')) {
          obj['partitionsAllOk'] = _ApiClient["default"].convertToType(data['partitionsAllOk'], 'Number');
        }
        if (data.hasOwnProperty('partitionsQuorum')) {
          obj['partitionsQuorum'] = _ApiClient["default"].convertToType(data['partitionsQuorum'], 'Number');
        }
        if (data.hasOwnProperty('status')) {
          obj['status'] = _ApiClient["default"].convertToType(data['status'], 'String');
        }
        if (data.hasOwnProperty('storageNodes')) {
          obj['storageNodes'] = _ApiClient["default"].convertToType(data['storageNodes'], 'Number');
        }
        if (data.hasOwnProperty('storageNodesUp')) {
          obj['storageNodesUp'] = _ApiClient["default"].convertToType(data['storageNodesUp'], 'Number');
        }
      }
      return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetClusterHealthResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetClusterHealthResponse</code>.
     */
  }, {
    key: "validateJSON",
    value: function validateJSON(data) {
      // check to make sure all required properties are present in the JSON string
      var _iterator = _createForOfIteratorHelper(GetClusterHealthResponse.RequiredProperties),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var property = _step.value;
          if (!data.hasOwnProperty(property)) {
            throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
          }
        }
        // ensure the json data is a string
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
        throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
      }
      return true;
    }
  }]);
}();
GetClusterHealthResponse.RequiredProperties = ["connectedNodes", "knownNodes", "partitions", "partitionsAllOk", "partitionsQuorum", "status", "storageNodes", "storageNodesUp"];

/**
 * the nubmer of nodes this Garage node currently has an open connection to
 * @member {Number} connectedNodes
 */
GetClusterHealthResponse.prototype['connectedNodes'] = undefined;

/**
 * the number of nodes this Garage node has had a TCP connection to since the daemon started
 * @member {Number} knownNodes
 */
GetClusterHealthResponse.prototype['knownNodes'] = undefined;

/**
 * the total number of partitions of the data (currently always 256)
 * @member {Number} partitions
 */
GetClusterHealthResponse.prototype['partitions'] = undefined;

/**
 * the number of partitions for which we are connected to all storage nodes responsible of storing it
 * @member {Number} partitionsAllOk
 */
GetClusterHealthResponse.prototype['partitionsAllOk'] = undefined;

/**
 * the number of partitions for which a quorum of write nodes is available
 * @member {Number} partitionsQuorum
 */
GetClusterHealthResponse.prototype['partitionsQuorum'] = undefined;

/**
 * One of `healthy`, `degraded` or `unavailable`: - `healthy`: Garage node is connected to all storage nodes - `degraded`: Garage node is not connected to all storage nodes, but a quorum of write nodes is available for all partitions - `unavailable`: a quorum of write nodes is not available for some partitions
 * @member {String} status
 */
GetClusterHealthResponse.prototype['status'] = undefined;

/**
 * the number of storage nodes currently registered in the cluster layout
 * @member {Number} storageNodes
 */
GetClusterHealthResponse.prototype['storageNodes'] = undefined;

/**
 * the number of storage nodes to which a connection is currently open
 * @member {Number} storageNodesUp
 */
GetClusterHealthResponse.prototype['storageNodesUp'] = undefined;
var _default = exports["default"] = GetClusterHealthResponse;